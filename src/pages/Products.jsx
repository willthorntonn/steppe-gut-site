import PageHeader from "../components/ui/PageHeader";
import PageMeta from "../components/ui/PageMeta";

// STUB — routing skeleton only (Stage 1). Built out in a later stage.
export default function Products() {
  return (
    <>
      <PageMeta title="Products · Steppe Gut" noindex />
      <PageHeader eyebrow="Coming next" title="Products" lead="Route `/products/` is wired. This page is built in a later stage." />
    </>
  );
}
