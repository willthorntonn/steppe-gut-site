import PageHeader from "../components/ui/PageHeader";
import PageMeta from "../components/ui/PageMeta";

// STUB — routing skeleton only (Stage 1). Built out in a later stage.
export default function Cart() {
  return (
    <>
      <PageMeta title="Your basket · Steppe Gut" noindex />
      <PageHeader eyebrow="Coming next" title="Your basket" lead="Route `/cart/` is wired. This page is built in a later stage." />
    </>
  );
}
