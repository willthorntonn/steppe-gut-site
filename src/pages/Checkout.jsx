import PageHeader from "../components/ui/PageHeader";
import PageMeta from "../components/ui/PageMeta";

// STUB — routing skeleton only (Stage 1). Built out in a later stage.
export default function Checkout() {
  return (
    <>
      <PageMeta title="Checkout · Steppe Gut" noindex />
      <PageHeader eyebrow="Coming next" title="Checkout" lead="Route `/checkout/` is wired. This page is built in a later stage." />
    </>
  );
}
