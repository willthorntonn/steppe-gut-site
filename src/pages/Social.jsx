import PageHeader from "../components/ui/PageHeader";
import PageMeta from "../components/ui/PageMeta";

// STUB — routing skeleton only (Stage 1). Built out in a later stage.
export default function Social() {
  return (
    <>
      <PageMeta title="Social · Steppe Gut" noindex />
      <PageHeader eyebrow="Coming next" title="Social" lead="Route `/social/` is wired. This page is built in a later stage." />
    </>
  );
}
